CREATE TABLE BOOKING
(
    BOOKCODE DECIMAL(2) PRIMARY KEY,
    ROOMCODE DECIMAL(2),
    PERSON   DECIMAL(2),
    CHECKIN  CHAR(10),
    CHECKOUT CHAR(10),
    NAME     VARCHAR2(20),
    MOBILE   VARCHAR2(20)
);
